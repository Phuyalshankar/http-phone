'use strict';

/**
 * Components barrel — export all reusable UI components.
 * Import example: const { MyButton } = require('./components');
 */

// Add your custom components here:
// const { MyButton } = require('./MyButton');

module.exports = {};
