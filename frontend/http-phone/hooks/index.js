'use strict';

/**
 * Hooks barrel — export all custom hooks.
 * Import example: const { useAppState } = require('./hooks');
 */

const { useAppState } = require('./useAppState');

module.exports = { useAppState };
